export enum registerErrorMessage {
    EMPTYINPUT = 'Empty input field/s',
    MISMATCHPASSWORDS = 'Passwords do not match',
    INVALIDEMAIL = 'Invalid Email Address',
    INVALIDPASSWORD = 'Invalid Password (Passwords must be 8 or more characters long)'
}

export enum commonLinkType {
	NAVLINK,
	LINK,
	LINKCLEAR,
	ANCHOR
}
